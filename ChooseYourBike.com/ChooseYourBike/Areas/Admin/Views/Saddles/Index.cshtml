@model IEnumerable<ChooseYourBike.Models.Saddle>

@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SaddleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SaddleType)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SaddleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SaddleType)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }
        <tr>
            <td>
                @Html.ActionLink("Create New", "Create")
            </td>
        </tr>
    </table>
</body>
</html>
